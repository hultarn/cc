-- https://pastebin.com/rmKyBcdA

while true do
    local peripheralLeft = peripheral.wrap("right")
    local item = peripheralLeft.getItemDetail(1)

    if item then
        peripheralLeft.pushItems("top", 1, nil, 1)
    end
    os.sleep(1)
end